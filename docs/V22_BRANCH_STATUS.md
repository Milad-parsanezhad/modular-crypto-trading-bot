# v0.22 ICT Vision Branch Status

Branch: `research/v22-ict-vision-lab`

Current completed milestone:

- deterministic eight-channel candlestick/ICT renderer implemented;
- twenty causal weak ICT structural labels implemented;
- source-bar and render SHA-256 audit trail implemented;
- future outcome kept separate from visual channels;
- custom CNN, small ViT, ResNet18 and EfficientNet-B0 adapters implemented;
- 6/6 v0.22 renderer/label integrity tests passed;
- first public 4h BTC/ETH/SOL smoke completed and scientifically rejected because structural F1 and outcome AUC were inadequate;
- v0.22b class-balanced raw-vs-Ichimoku-vs-structure ablation implemented with fail-closed promotion gates;
- v0.22b integrity tests passed; its heavier public-data model tournament is the active experiment at this snapshot.

No vision model is connected to a trading policy or RL agent at this stage.

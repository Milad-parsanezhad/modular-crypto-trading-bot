name: v38-soft-portfolio-RETIRED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Rejected scientific path
        run: |
          echo 'V38_SOFT_PORTFOLIO_PATH_REJECTED_PRE_EXECUTION'
          echo 'This branch is retained only as an auditable rejected path.'
          echo 'Authoritative replacement: research/v38-temporal-consensus'
          echo 'No Kraken, PAPER replacement, or LIVE authorization is permitted from this workflow.'
          exit 1
